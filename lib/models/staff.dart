class Staff {
  String uid;
  String name;
  String email;
  String mobileNo;
  String jobTitle;
  String section;
  bool activeStatus;
  bool userEnabled;

  Staff({this.uid, this.name, this.email, this.mobileNo, this.jobTitle,
      this.section, this.activeStatus, this.userEnabled});
}